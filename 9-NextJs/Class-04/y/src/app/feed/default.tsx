import Page from "./page";

export default Page;

// page.tsx - defines the UI for the route (the main page of the route).
// layout.tsx - defines layout wrapping for that route.
// default.tsx - defines the default UI for a slot in a parallel route (fallback).
// loading.tsx - defines loading UI while fetching.
// error.tsx - defines error boundary UI.
// not-found.tsx - defines the 404 UI for that segment.
